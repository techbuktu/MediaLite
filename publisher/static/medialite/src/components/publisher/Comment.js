import React, { Component } from 'react'

class Comment extends Component {
    
    render() {
        return (
            <div>
                UI for a posted comment.
            </div>
        )
    }
}

export default Comment;